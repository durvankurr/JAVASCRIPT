## Practice

This practice comprises 6 exercises

### Problem Statements

#### Practice 1 - Evaluate Arithmetic Expressions

Evaluate the given arithmetic expressions and provide the results in the `p1-submission.csv` file. 

The file is located inside the folder `p1-evaluate-arithemetic-expressions`.

#### Practice 2 - Write Assignment Statements

Write assignment statements for the stated requirements in the `p2-submission.csv` file. 

The file is located inside the folder `p2-write-assignment-statements`.

#### Practice 3 - Predict Output and it’s Data Type

The file `p3-submission.csv` contains expressions that need to be evaluated. Predict the output of these expressions and specify the type of output in the same file.

The file is located inside the folder `p3-predict-output-and-its-datatype`.

#### Practice 4 - Identify Good and Bad Variable Names

For the given variable names (identifiers), identify the variable names that follow the naming rules, naming conventions and are meaningful. 

Specify the good variable names and bad variable names in `p4-submission.csv`. 

Also specify the reason, why the variable name is a bad variable name.

The file is located inside the folder `p4-identify-good-and-bad-variable-names`.

#### Practice 5 - Enquiry Form

A bank has an inquiry form that customers need to fill at when they open a bank account. ​

​Write assignment statements using JavaScript that will assign the given values to the variables based on the datatype specified

Variable Name | Value | Data Type
|-|-|-| 
name | Jay Sharma | String
contactNumber ​| +914123125569​ | Number
email​ | jay.sharma@gmail.com​ | String​
age | 34 | Number |
gender | M | String
occupation | Manager | String |
annualSalary  | Rs. 10,00,000 | Number |
nationality  | Indian | String |
isMarried | true | boolean |

The solution for this exercise should be provided in `p5-submission.js` file.

The file is located inside the folder `p5-enquiry-form`.

#### Practice 6 - Calculate Revised Selling Price

A gift store owner wants to increase the selling price of items by 5% due to a hike in the price of raw materials.​

​Write a JavaScript program that calculates the revised selling price of the item given its current selling price.​

Declare and initialize variables as shown in the table below:

Variable Name | Value | Data Type
|-|-|-| 
itemName | Kaisa Bowl​ | String
sellingPrice | Rs. 109.90​ | Number

Calculate the revised selling price for the given item and store it in a variable

`revisedSellingPrice = sellingPrice * 1.05;`

Display the revised selling price using template literals.​

Sample Output: ​
```
Revised Selling Price for Rounded Kaisa Bowl is Rs. 115.395​.